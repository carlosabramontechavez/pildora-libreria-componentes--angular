import { ReactiveFormDirective } from './reactive-form.directive';

describe('ReactiveFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactiveFormDirective();
    expect(directive).toBeTruthy();
  });
});
